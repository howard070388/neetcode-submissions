class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        save =[]
        for num in nums:
            save.append(num)
        for num in nums:
            save.append(num)
        return save